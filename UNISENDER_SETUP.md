# 📧 Настройка UniSender для Email

## ✅ UniSender уже интегрирован в проект!

UniSender — российский сервис email-рассылок:
- 🇷🇺 Российская компания
- ✅ **1500 писем/месяц бесплатно** (50/день)
- ✅ Простой REST API
- ✅ Отправка от вашего домена

---

## 🚀 Быстрая настройка (5 минут):

### Шаг 1: Регистрация
1. Открой: https://www.unisender.com/ru/
2. Нажми **"Попробовать бесплатно"**
3. Зарегистрируйся (email + пароль)

### Шаг 2: Получи API ключ
1. В личном кабинете найди: **Настройки → Интеграция → API**
2. Или прямая ссылка: https://www.unisender.com/ru/personal/api/
3. Нажми **"Создать ключ API"**
4. Скопируй ключ (длинная строка типа `abc123def456...`)

### Шаг 3: Верифицируй email отправителя
1. Зайди в: **Настройки → Адреса отправителей**
2. Нажми **"Добавить адрес"**
3. Введи: `info@hostel-delas.ru`
4. Укажи имя: `Хостел DELAS`
5. Подтверди через email (придёт письмо с ссылкой)

### Шаг 4: Добавь переменные в TimeWeb

В настройках контейнера TimeWeb Cloud добавь:

```
UNISENDER_API_KEY=твой_api_ключ_здесь
EMAIL_PROVIDER=unisender
SMTP_USER=info@hostel-delas.ru
```

**Важно:** `SMTP_USER` нужен чтобы указать от какого email отправлять (должен совпадать с верифицированным в UniSender).

### Шаг 5: Перезапусти контейнер

После добавления переменных **перезапусти контейнер** в TimeWeb.

### Шаг 6: Проверь работу

Открой в браузере:
```
https://hostel-delas.ru/api/test-email?test=true
```

Должен вернуться:
```json
{
  "success": true,
  "message": "UniSender API подключение успешно"
}
```

Отправь тестовое письмо:
```
https://hostel-delas.ru/api/test-email?to=твой@email.com
```

---

## 🔧 Структура кода

### Файлы:
- `src/lib/unisender.ts` — UniSender API интеграция
- `src/lib/email.ts` — использует UniSender если `EMAIL_PROVIDER=unisender`

### Как работает:

```typescript
// Автоматически выбирает провайдера
await sendEmail({
  to: 'guest@example.com',
  subject: 'Подтверждение бронирования',
  html: '<h1>Спасибо за бронирование!</h1>',
})

// Если EMAIL_PROVIDER=unisender → UniSender API
// Если EMAIL_PROVIDER=smtp → SMTP (не работает на TimeWeb)
```

---

## 🆘 Решение проблем

### Ошибка: "API key не настроен"
Проверь что `UNISENDER_API_KEY` добавлен в переменные TimeWeb и контейнер перезапущен.

### Ошибка: "Sender email not verified"
Нужно верифицировать `info@hostel-delas.ru` в личном кабинете UniSender (Настройки → Адреса отправителей).

### Ошибка: "Invalid API key"
Проверь что скопировал API ключ полностью (без пробелов в начале/конце).

### Письма не приходят
1. Проверь что email верифицирован в UniSender
2. Проверь спам-папку
3. Посмотри логи в TimeWeb (там будут ошибки от UniSender API)

---

## 📊 Лимиты

### Бесплатный план:
- **1500 писем/месяц**
- **50 писем/день**
- Без ограничений на получателей

### Платные планы (если понадобится):
- От **200₽/месяц** за 3000 писем
- От **400₽/месяц** за 10,000 писем

Для хостела бесплатного плана хватит на несколько месяцев работы!

---

## 🎉 Готово!

После настройки:
- ✅ Гостям будут приходить подтверждения бронирования
- ✅ Письма об отмене/изменениях
- ✅ Отправка от `info@hostel-delas.ru`
- ✅ Все работает через HTTP (TimeWeb не блокирует)

Если что-то не получается — пиши, помогу! 🚀

