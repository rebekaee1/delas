# 🚀 Инструкция по запуску в Production (TimeWeb Cloud)

## Шаг 1: Создание базы данных PostgreSQL

### 1.1 Создать БД в TimeWeb Cloud

1. Войди в [панель управления TimeWeb Cloud](https://timeweb.cloud/)
2. Перейди в раздел **"Базы данных"** → **"Создать"**
3. Выбери **PostgreSQL**
4. Выбери конфигурацию:
   - **Минимальная** (1 CPU, 1 GB RAM) — достаточно для старта
   - Регион: Россия
5. Задай имя базы: `delas_prod`
6. Задай имя пользователя и пароль '6CNQs5Da&iQzvH'
7. Нажми **"Создать"**

### 1.2 Получить строку подключения

После создания БД скопируй данные для подключения:

```
DATABASE_URL="postgresql://ПОЛЬЗОВАТЕЛЬ:ПАРОЛЬ@ХОСТ:5432/delas_prod?sslmode=require"
```

Пример:
```
DATABASE_URL="postgresql://delas_user:MyPassword123@pg-xxxxx.timeweb.cloud:5432/delas_prod?sslmode=require"
```

---

## Шаг 2: Создание приложения в App Platform

### 2.1 Создать приложение

1. В панели TimeWeb Cloud перейди в **"App Platform"** → **"Создать"**
2. Выбери тип: **Next.js**
3. Выбери версию Node.js: **20.x** (или последнюю LTS)

### 2.2 Подключить репозиторий

1. Нажми **"GitHub"** и авторизуйся
2. Выбери репозиторий `rebekaee1/delas`
3. Выбери ветку: `main`
4. ✅ Включи **"Сборка по последнему коммиту"** (автодеплой)

### 2.3 Настройка сборки

| Параметр | Значение |
|----------|----------|
| Команда сборки | `npm run build:prod` |
| Команда запуска | `npm run start` |
| Порт | `3000` |
| Директория сборки | `.next` |

### 2.4 Добавить переменные окружения

Нажми **"Добавить переменную"** и добавь каждую:

| Название | Значение | Обязательно |
|----------|----------|-------------|
| `DATABASE_URL` | Строка подключения из Шага 1.2 | ✅ |
| `YOOKASSA_SHOP_ID` | Твой shopId из ЮKassa | ✅ |
| `YOOKASSA_SECRET_KEY` | Боевой ключ `live_...` | ✅ |
| `TELEGRAM_BOT_TOKEN` | Токен твоего бота | ✅ |
| `TELEGRAM_CHAT_ID` | `433221767` | ✅ |
| `NEXT_PUBLIC_SITE_URL` | `https://delas-sochi.ru` | ✅ |
| `SMTP_HOST` | `smtp.timeweb.ru` | ✅ |
| `SMTP_PORT` | `465` | ✅ |
| `SMTP_USER` | `info@hostel-delas.ru` | ✅ |
| `SMTP_PASSWORD` | Пароль от почты | ✅ |
| `SMTP_FROM` | `info@hostel-delas.ru` | ✅ |
| `NEXT_PUBLIC_ADMIN_SECRET` | Секретный ключ для /admin/quick-booking | Рекомендуется |
| `NEXT_PUBLIC_YANDEX_METRIKA_ID` | ID счётчика метрики | По желанию |

### 2.5 Выбрать конфигурацию

- **Регион:** Россия (Москва или Санкт-Петербург)
- **Конфигурация:** Минимум 1 CPU, 1 GB RAM

### 2.6 Запустить деплой

1. Нажми **"Запустить деплой"**
2. Подожди 3-5 минут
3. После завершения получишь URL вида `delas-xxxxx.tw1.ru`

---

## Шаг 3: Инициализация базы данных

### 3.1 Через терминал App Platform

1. В App Platform перейди во вкладку **"Терминал"** (или SSH)
2. Выполни команды:

```bash
npx prisma db push
npx prisma db seed
```

### 3.2 Альтернатива: локально

Если терминал недоступен, выполни локально с production DATABASE_URL:

```bash
# Временно установи переменную
export DATABASE_URL="postgresql://ПОЛЬЗОВАТЕЛЬ:ПАРОЛЬ@ХОСТ:5432/delas_prod?sslmode=require"

# Применить схему
npx prisma db push

# Заполнить данные
npx prisma db seed
```

---

## Шаг 4: Подключение домена

### 4.1 В TimeWeb Cloud

1. В настройках приложения перейди в **"Домены"**
2. Нажми **"Добавить домен"**
3. Введи: `delas-sochi.ru`
4. TimeWeb покажет DNS-записи для настройки

### 4.2 У регистратора домена

Добавь DNS-записи (TimeWeb покажет точные значения):

```
Тип: A
Имя: @
Значение: IP от TimeWeb

Тип: CNAME
Имя: www
Значение: delas-xxxxx.tw1.ru
```

### 4.3 SSL-сертификат

TimeWeb **автоматически** выпустит бесплатный SSL-сертификат Let's Encrypt после привязки домена.

---

## Шаг 5: Настройка ЮKassa

### 5.1 Переключение в боевой режим

1. Войди в [ЮKassa](https://yookassa.ru/my)
2. Если магазин в тестовом режиме → подай заявку на подключение
3. После одобрения получишь **боевые ключи** (начинаются с `live_`)

### 5.2 Настройка Webhook

1. В ЮKassa → **"Интеграция"** → **"HTTP-уведомления"**
2. Нажми **"Добавить URL"**
3. Введи: `https://delas-sochi.ru/api/payment/webhook`
4. Выбери события:
   - ✅ `payment.succeeded` — успешная оплата
   - ✅ `payment.canceled` — отмена платежа
   - ✅ `refund.succeeded` — успешный возврат

### 5.3 Обновить ключи

После получения боевых ключей обнови в TimeWeb Cloud:
- `YOOKASSA_SHOP_ID` → твой shopId
- `YOOKASSA_SECRET_KEY` → ключ `live_...`

---

## Шаг 6: Проверка после деплоя

### 6.1 Проверка страниц
- [ ] Главная: `https://delas-sochi.ru`
- [ ] Номера: `https://delas-sochi.ru/rooms`
- [ ] Бронирование: `https://delas-sochi.ru/booking`
- [ ] Оферта: `https://delas-sochi.ru/terms`
- [ ] Политика ПД: `https://delas-sochi.ru/privacy`
- [ ] Корп. клиенты: `https://delas-sochi.ru/corporate`

### 6.2 Тестовое бронирование
1. Заполни форму бронирования
2. Перейди на оплату в ЮKassa
3. Оплати (тестовой картой если ещё тест-режим)
4. Проверь:
   - [ ] Уведомление в Telegram пришло
   - [ ] Письмо на email пришло
   - [ ] Статус в базе = CONFIRMED

### 6.3 SEO
- [ ] `https://delas-sochi.ru/sitemap.xml` — доступен
- [ ] `https://delas-sochi.ru/robots.txt` — доступен

### 6.4 Мобильная версия
- [ ] Сайт корректно отображается на телефоне
- [ ] Меню работает
- [ ] Форма бронирования удобна

---

## Шаг 7: Быстрое заселение (админка)

Для ручного добавления гостей (безнал, на месте):

```
https://delas-sochi.ru/admin/quick-booking?key=ТВОЙ_СЕКРЕТНЫЙ_КЛЮЧ
```

Секретный ключ задаётся в переменной `NEXT_PUBLIC_ADMIN_SECRET`.

💡 **Совет:** Сохрани эту ссылку в закладки телефона для быстрого доступа.

---

## 📊 Мониторинг

### Логи в TimeWeb Cloud
1. App Platform → твоё приложение
2. Вкладка **"Логи"** — здесь видны все ошибки и запросы

### Яндекс.Метрика
1. Зайди на [metrika.yandex.ru](https://metrika.yandex.ru)
2. Создай счётчик для `delas-sochi.ru`
3. Скопируй ID в переменную `NEXT_PUBLIC_YANDEX_METRIKA_ID`
4. Сделай редеплой

### Яндекс.Вебмастер
1. Зайди на [webmaster.yandex.ru](https://webmaster.yandex.ru)
2. Добавь сайт `delas-sochi.ru`
3. Подтверди владение (через DNS или HTML-файл)
4. Отправь sitemap: `https://delas-sochi.ru/sitemap.xml`

---

## 🆘 Решение проблем

### Webhook ЮKassa не приходит
1. Проверь URL в настройках ЮKassa (должен быть с https://)
2. Проверь логи в TimeWeb Cloud
3. Убедись что домен работает

### База данных недоступна
1. Проверь `DATABASE_URL` — правильные хост, порт, пользователь, пароль
2. Проверь что БД запущена в TimeWeb Cloud
3. Проверь что добавлен `?sslmode=require` в конце строки

### Telegram не отправляет
1. Проверь `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`
2. Напиши боту `/start` в личку
3. Если бот в группе — убедись что он админ

### Email не отправляется
1. Проверь SMTP настройки (хост, порт, логин, пароль)
2. Если TimeWeb блокирует 465 порт — попробуй 587
3. Проверь правильность пароля от почты

### Приложение не стартует
1. Проверь логи в TimeWeb Cloud
2. Убедись что все обязательные переменные заданы
3. Попробуй сделать редеплой

---

## 🔐 Безопасность

⚠️ **ВАЖНО:**

1. **Никогда** не публикуй секретные ключи в git
2. Используй только **боевые** ключи ЮKassa на проде (`live_...`)
3. HTTPS обязателен — ЮKassa не работает без него
4. Регулярно обновляй зависимости: `npm update`
5. Меняй `NEXT_PUBLIC_ADMIN_SECRET` на что-то сложное

---

## 📞 Контакты поддержки

| Сервис | Контакт |
|--------|---------|
| **TimeWeb Cloud** | support@timeweb.ru, 8-800-707-33-96 |
| **ЮKassa** | support@yookassa.ru, 8-800-250-66-99 |

---

## 🔄 Автоматический деплой

После настройки каждый `git push` в ветку `main` автоматически:
1. Запустит сборку
2. Задеплоит новую версию
3. Перезапустит приложение

Статус деплоя можно отслеживать во вкладке **"Деплой"** в App Platform.

---

**Удачи с запуском! 🎉**
