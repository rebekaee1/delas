# 🚀 Инструкция по запуску в Production

## Вариант 1: Vercel (рекомендую) — Бесплатно

Самый простой и быстрый способ. Vercel — это платформа от создателей Next.js.

### Шаг 1: Регистрация и подключение репозитория

1. Зайди на [vercel.com](https://vercel.com)
2. Нажми "Sign Up" → выбери "Continue with GitHub"
3. После входа нажми "Add New Project"
4. Выбери репозиторий `rebekaee1/delas`
5. Нажми "Import"

### Шаг 2: Настройка переменных окружения

На экране настройки проекта найди раздел **"Environment Variables"** и добавь:

| Название | Значение | Описание |
|----------|----------|----------|
| `DATABASE_URL` | `postgresql://...` | URL твоей production базы (см. Шаг 3) |
| `YOOKASSA_SHOP_ID` | Твой shopId | Из личного кабинета ЮKassa |
| `YOOKASSA_SECRET_KEY` | `live_...` | **⚠️ Боевой ключ!** (начинается с `live_`) |
| `TELEGRAM_BOT_TOKEN` | Твой токен | Токен бота |
| `TELEGRAM_CHAT_ID` | `433221767` | Твой chat_id |
| `NEXT_PUBLIC_SITE_URL` | `https://delas-sochi.ru` | Твой домен с https:// |
| `SMTP_HOST` | `smtp.timeweb.ru` | SMTP сервер |
| `SMTP_PORT` | `465` | Порт |
| `SMTP_USER` | `info@hostel-delas.ru` | Email |
| `SMTP_PASSWORD` | Твой пароль | Пароль от почты |
| `SMTP_FROM` | `info@hostel-delas.ru` | От кого письма |
| `SENTRY_DSN` | (опционально) | Если настроишь Sentry |
| `NEXT_PUBLIC_YANDEX_METRIKA_ID` | (опционально) | ID Яндекс.Метрики |

### Шаг 3: База данных PostgreSQL

**Вариант A: Neon (рекомендую, бесплатно)**
1. Зайди на [neon.tech](https://neon.tech)
2. Создай аккаунт
3. Создай новый проект "delas"
4. Скопируй connection string → вставь в `DATABASE_URL`

**Вариант B: Supabase (бесплатно)**
1. Зайди на [supabase.com](https://supabase.com)
2. Создай проект
3. Settings → Database → Connection string → вставь в `DATABASE_URL`

**Вариант C: TimeWeb Cloud PostgreSQL**
1. Зайди в [TimeWeb Cloud](https://timeweb.cloud)
2. Создай базу данных PostgreSQL
3. Используй connection string

### Шаг 4: Деплой

1. Нажми "Deploy"
2. Подожди 2-3 минуты
3. После деплоя получишь URL вида `delas.vercel.app`

### Шаг 5: Подключение домена

1. В проекте Vercel → Settings → Domains
2. Нажми "Add Domain"
3. Введи `delas-sochi.ru`
4. Vercel покажет DNS записи для настройки

**В панели регистратора домена добавь:**
```
Тип: A
Имя: @
Значение: 76.76.21.21

Тип: CNAME
Имя: www
Значение: cname.vercel-dns.com
```

### Шаг 6: Инициализация базы данных

После первого деплоя выполни в терминале Vercel (или локально с production DATABASE_URL):

```bash
npx prisma db push
npx prisma db seed
```

Или через Vercel CLI:
```bash
vercel env pull .env.production.local
npx prisma db push
npx prisma db seed
```

---

## Вариант 2: TimeWeb Cloud App Platform

### Шаг 1: Регистрация
1. Зайди на [timeweb.cloud](https://timeweb.cloud)
2. Зарегистрируйся / войди

### Шаг 2: Создание приложения
1. Панель управления → Приложения → "Создать"
2. Выбери "Next.js"
3. Подключи GitHub репозиторий

### Шаг 3: Настройка
1. Build command: `npm run build:prod`
2. Start command: `npm run start`
3. Port: `3000`
4. Добавь все переменные окружения (как в Vercel)

### Шаг 4: База данных
1. В TimeWeb Cloud создай PostgreSQL
2. Скопируй connection string в `DATABASE_URL`

### Шаг 5: Домен
1. Настройки приложения → Домены
2. Добавь `delas-sochi.ru`
3. Настрой DNS записи

---

## 📋 После деплоя: Настройка ЮKassa

### 1. Переключение на боевой режим

1. Войди в [ЮKassa](https://yookassa.ru/my)
2. Перейди в раздел "Интеграция"
3. Если магазин в тестовом режиме — подай заявку на подключение

### 2. Настройка Webhook

1. Интеграция → HTTP-уведомления
2. Нажми "Добавить URL"
3. Введи: `https://delas-sochi.ru/api/payment/webhook`
4. Выбери события:
   - ✅ `payment.succeeded`
   - ✅ `payment.canceled`
   - ✅ `refund.succeeded`

### 3. Получение боевых ключей

1. В личном кабинете ЮKassa → Интеграция → API ключи
2. Скопируй `shopId` и `secretKey` (начинается с `live_`)
3. Обнови эти значения в переменных окружения на хостинге

---

## ✅ Чеклист проверки после деплоя

### 1. Проверка страниц
- [ ] Главная страница загружается
- [ ] /rooms — список номеров отображается
- [ ] /booking — форма бронирования работает
- [ ] /terms — оферта доступна
- [ ] /privacy — политика конфиденциальности
- [ ] /corporate — страница корп. клиентов

### 2. Тестовое бронирование
- [ ] Заполнить форму бронирования
- [ ] Перейти на оплату (должен открыться ЮKassa)
- [ ] Оплатить тестовой картой (если ещё тестовый режим)
- [ ] Проверить получение уведомления в Telegram
- [ ] Проверить получение письма на email

### 3. Мобильная версия
- [ ] Сайт отображается корректно на телефоне
- [ ] Меню работает
- [ ] Форма бронирования удобна

### 4. SEO
- [ ] /sitemap.xml доступен
- [ ] /robots.txt доступен
- [ ] Метатеги корректны (проверь через [metatags.io](https://metatags.io))

---

## 🔐 Безопасность

⚠️ **ВАЖНО:**

1. **Никогда** не коммить `.env.local` в git
2. Использовать только `live_` ключи ЮKassa на проде
3. HTTPS обязателен (ЮKassa не работает без него)
4. Регулярно обновлять зависимости

---

## 📊 Мониторинг (опционально)

### Sentry (отслеживание ошибок)
1. Зарегистрируйся на [sentry.io](https://sentry.io)
2. Создай проект Next.js
3. Скопируй DSN в `SENTRY_DSN`

### Яндекс.Метрика
1. Зарегистрируйся на [metrika.yandex.ru](https://metrika.yandex.ru)
2. Создай счётчик
3. Скопируй ID в `NEXT_PUBLIC_YANDEX_METRIKA_ID`

---

## 🆘 Если что-то не работает

### Webhook ЮKassa не приходит
1. Проверь URL в настройках ЮKassa
2. Проверь что домен работает по HTTPS
3. Проверь логи в Vercel/TimeWeb

### Ошибка базы данных
1. Проверь `DATABASE_URL`
2. Выполни `npx prisma db push`
3. Проверь что база доступна извне

### Telegram не отправляет
1. Проверь `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`
2. Убедись что бот добавлен в чат/группу
3. Напиши боту `/start`

### Email не отправляется
1. Проверь SMTP настройки
2. Проверь что порт 465 разрешён
3. Проверь пароль приложения (если двухфакторная авторизация)

---

## 📞 Контакты поддержки

- **ЮKassa:** support@yookassa.ru, +7 800 250-66-99
- **Vercel:** support.vercel.com
- **TimeWeb:** support.timeweb.ru

---

## Быстрый деплой через Vercel (одной кнопкой)

Если репозиторий публичный, можно добавить кнопку в README:

```markdown
[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/rebekaee1/delas)
```

---

**Удачи с запуском! 🎉**

