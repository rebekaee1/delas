# 🚀 Чеклист готовности к Production

## ✅ Что уже сделано

- [x] Интеграция с ЮKassa (создание платежей, webhook, статус)
- [x] Telegram уведомления (бронирования, оплата, корп. заявки)
- [x] База данных Prisma (схема, миграции, seed)
- [x] Соответствие 152-ФЗ (согласие ПД, политика конфиденциальности)
- [x] Соответствие 54-ФЗ (онлайн-чеки в ЮKassa)
- [x] Cookie consent баннер
- [x] Публичная оферта (/terms)
- [x] Unit и интеграционные тесты

---

## 📋 Перед деплоем (локально)

### 1. Удалить тестовые эндпоинты
```bash
rm -rf src/app/api/test/
```

### 2. Проверить .env.example
Создать файл `.env.example` с шаблоном для production:

```env
# База данных (Production)
DATABASE_URL="postgresql://user:password@host:5432/delas_prod"

# ЮKassa (боевые ключи!)
YOOKASSA_SHOP_ID="ваш_shop_id"
YOOKASSA_SECRET_KEY="live_ваш_секретный_ключ"

# Telegram
TELEGRAM_BOT_TOKEN="ваш_токен_бота"
TELEGRAM_CHAT_ID="ваш_chat_id"

# Сайт
NEXT_PUBLIC_SITE_URL="https://ваш-домен.ru"

# Email (Resend) - опционально
RESEND_API_KEY=""
FROM_EMAIL="noreply@ваш-домен.ru"
```

### 3. Сборка проекта
```bash
npm run build
```
Убедиться что нет ошибок.

---

## 🌐 При деплое на хостинг

### 1. Настройка переменных окружения
Установить ВСЕ переменные из `.env.example` в панели хостинга.

**⚠️ ВАЖНО:**
- `YOOKASSA_SECRET_KEY` — использовать `live_` ключ (не `test_`)
- `NEXT_PUBLIC_SITE_URL` — полный URL с https://

### 2. База данных
```bash
# Применить схему к production БД
npx prisma db push

# Заполнить начальные данные
npx prisma db seed
```

### 3. Настройка webhook в ЮKassa

1. Зайти в [Личный кабинет ЮKassa](https://yookassa.ru/my)
2. Интеграция → HTTP-уведомления
3. Добавить URL: `https://ваш-домен.ru/api/payment/webhook`
4. Выбрать события:
   - ✅ `payment.succeeded`
   - ✅ `payment.canceled`
   - ✅ `payment.waiting_for_capture`

### 4. HTTPS
Убедиться что сайт работает по HTTPS (ЮKassa требует).

### 5. DNS
Настроить домен на IP хостинга.

---

## 🔍 Проверка после деплоя

### 1. Тестовое бронирование
- [ ] Создать бронирование на сайте
- [ ] Убедиться что редирект на ЮKassa работает
- [ ] Оплатить тестовой картой
- [ ] Проверить webhook (статус брони = CONFIRMED)
- [ ] Проверить Telegram уведомление

### 2. Проверка страниц
- [ ] Главная страница загружается
- [ ] /rooms — список номеров
- [ ] /booking — форма бронирования
- [ ] /terms — оферта
- [ ] /privacy — политика ПД
- [ ] /corporate — корп. клиенты

### 3. Мобильная версия
- [ ] Сайт корректно отображается на телефоне
- [ ] Форма бронирования работает

---

## ⚠️ Потенциальные проблемы

### 1. Дублирование платежей
**Защита:** Используется `Idempotence-Key` в ЮKassa API — один и тот же запрос не создаст два платежа.

### 2. Webhook не доходит
**Решение:** 
- Проверить URL в настройках ЮKassa
- Проверить логи сервера
- Использовать `/api/payment/sync` для ручной синхронизации (временно)

### 3. Несоответствие статусов
**Защита:** Webhook обновляет статус в БД. Если не сработал — `/api/payment/status` синхронизирует.

### 4. Истёкшие платежи
ЮKassa автоматически отменяет платежи через 1 час если не оплачены.

---

## 📊 Мониторинг

### Рекомендуется настроить:
1. **Логирование ошибок** (Sentry, LogRocket)
2. **Алерты** если webhook возвращает ошибку
3. **Ежедневный отчёт** о бронированиях в Telegram

---

## 🔐 Безопасность

- [ ] `.env.local` НЕ попадает в git (проверить .gitignore)
- [ ] Секретные ключи только на сервере
- [ ] HTTPS обязателен
- [ ] Регулярное обновление зависимостей

---

## Контакты поддержки

- **ЮKassa:** support@yookassa.ru
- **Telegram Bot API:** https://core.telegram.org/bots/api

